function soma (x, y) {
    return x + y;
}
console.log(soma(5, 4));

const multiplicacao = function(x, y) {
    return x * y;
}
console.log(multiplicacao(5, 4)); 

// com arrow
const dividir = (x, y) => { 
    return x / y;
}
console.log(dividir(10, 2));

const objeto = () => ({nome: 'CsS', sobrenome: 'sSalvador' });

